---
- name: Remove all running servers
  hosts: all
  tasks: 
    - name: Remove all servers and database
      package:
        name: httpd
        state: absent
      register: server removed
    - name: confirm operation successful
      debug: server removed
  